go 1.22

// Each Go tool is its own module, joined here into one workspace so they
// build together and can share future local helper modules.
// Add tools as you create them, e.g.:
//   use ./envcheck
